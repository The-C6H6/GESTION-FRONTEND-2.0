[project]
name = "esiqie-dictamenes"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "flet>=0.86.5",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
